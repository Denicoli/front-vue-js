import { createStore } from 'vuex'
import Api from './store/api'

export const Store = createStore({
    modules: {
        api: Api
    },
    state: {
    },
    mutations: {
    },
    actions: {
    },
    getters: {
    }
})
